fastapi>=0.115
uvicorn[standard]>=0.34

# Phase 2 — CPU-first: faster-whisper nutzt CTranslate2, KEIN PyTorch/CUDA nötig.
faster-whisper>=1.1

# GPU/Schweizerdeutsch später (RTX 5080 = Blackwell sm_120):
#   - faster-whisper mit device="cuda" braucht passende CTranslate2/cuDNN-Wheels.
#   - Eigener ZH-Whisper-Finetune dann via WHISPER_MODEL einhängen (siehe Spezifikation §9).
